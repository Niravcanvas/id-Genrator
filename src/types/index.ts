export interface IDCardData {
  name: string;
  email: string;
  role: string;
  company: string;
  phone: string;
  participantId: string;
  qrCodeDataURL: string;
}

export interface HackathonInfo {
  name: string;
  date: string;
  venue: string;
}